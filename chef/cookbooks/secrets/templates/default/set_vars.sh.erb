#!/bin/bash -v
    export AWS_ACCESS_KEY_ID=#{aws_id["aws_secret_key_id"]}
    export AWS_SECRET_KEY=#{aws_key["aws_secret_key"]}
    export TWILIO_SID==#{twilio_id["twilio_sid"]}
    export TWILIO_AUTH_TOKEN=#{twilio_token["twilio_auth_token"]}